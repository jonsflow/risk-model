// js/pages/divergence.js — Divergence page orchestrator (ES module stub).
// Full migration: imports Navigation for nav rendering;
// heavy rendering logic lives in app.js until full migration.

import { renderNav } from '../components/Navigation.js';
document.addEventListener('DOMContentLoaded', renderNav);
